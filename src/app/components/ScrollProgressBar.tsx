import { ScrollProgress } from "@/components/animate-ui/components/scroll-progress";

export default function ScrollProgressBar() {
  return <ScrollProgress progressProps={{ className: "bg-blue-500" }} />;
}
